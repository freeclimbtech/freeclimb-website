# Freeclimb Website

## Design Context

This project has `PRODUCT.md` (strategic: users, purpose, brand personality, anti-references, design principles) and `DESIGN.md` (visual: colors, typography, motion — currently a seed, pending real code). Read both before any design/build work; `/impeccable` commands load them automatically.

Register: brand. Platform: web. Purpose: convert visiting local service business owners into enquiries for Freeclimb's fixed-price web design packages.
